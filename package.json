{
  "name": "wallpaper-api",
  "version": "1.0.0",
  "description": "Dockerized wallpaper API with ratio-aware random selection and dedup window",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/static": "^8.2.0",
    "fastify": "^5.2.1",
    "image-size": "^1.2.1"
  }
}
